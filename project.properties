maven.compile.source=1.4
maven.compile.target=1.4
maven.javadoc.source=1.4

maven.checkstyle.format=sun

maven.xdoc.date = left
maven.xdoc.version = ${pom.currentVersion}

maven.repo.central=projects.sourceforge.net
maven.repo.central.directory=/home/groups/j/jf/jflac/htdocs/maven
maven.remote.group=jflac

maven.sourceforge.project.groupId=jflac

# need to fork the junit JVM to avoid JUnit's nasty delegating ClassLoader problems
#maven.junit.fork=true

# declare which properties to pass as JVM System Properites for the JUnit tests
# maven.junit.sysproperties is a " " (space) delimeted string of System Property names,
# whoose values should be defined elsewhere
#maven.junit.sysproperties=test.ldap.skiptests test.ldap.servername test.ldap.port test.ldap.baseDN test.ldap.login test.ldap.password test.ldap.selector

# you may want to override the following in your build.properties file
# (build.properties is your personal project configuration file, in the project directory -
# and NOT to be put under CVS control).
#test.ldap.skiptests=false
#test.ldap.servername=localhost
#test.ldap.port=389
#test.ldap.baseDN=ou=jsdsi,ou=test,dc=<MY-DOMAIN>,dc=<COM>
#test.ldap.login=cn=Manager,dc=<MY-DOMAIN>,dc=<COM>
#test.ldap.password=secret
#test.ldap.selector=CompatibleCertSelector|AuthCertSelector|SubjectCertSelector|NameCertSelector
